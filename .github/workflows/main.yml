name: Super Patcher V15 (Twin Mode)

on:
  workflow_dispatch:
    inputs:
      apk_url:
        description: 'Link File (APKS / XAPK Only)'
        required: true
        type: string
        default: 'https://www.mediafire.com/file/wzhnuu6jniby2oo/world.mnetplus_3.38.0.apks/file?dkey=4sagfzcstns&r=1771'

jobs:
  twin-patch-mode:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python 3.10
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' 

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y aapt zipalign apksigner zip unzip
          npm install -g apk-mitm
          pip install cloudscraper requests beautifulsoup4
          
          # Download LSPatch (Direct GitHub link works with wget)
          wget "https://github.com/LSPosed/LSPatch/releases/download/v0.6/jar-v0.6-398-release.jar" -O lspatch.jar

      - name: Create Certificate & Keystore
        run: |
          printf "%s\n" "-----BEGIN CERTIFICATE-----
          
          -----END CERTIFICATE-----" > reqable.pem
          
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

      - name: Download Target APK
        shell: python
        env:
          APK_URL: ${{ inputs.apk_url }}
        run: |
          import cloudscraper
          import os, sys
          from bs4 import BeautifulSoup

          url = os.environ['APK_URL']
          scraper = cloudscraper.create_scraper(browser='chrome')
          filename = "download.apks" 

          print(f"🔗 Processing URL: {url}")
          try:
              if 'mediafire.com' in url:
                  html = scraper.get(url).text
                  soup = BeautifulSoup(html, 'html.parser')
                  download_btn = soup.find('a', {'id': 'downloadButton'})
                  if download_btn:
                      url = download_btn.get('href')
          except Exception as e:
              print(f"⚠️ Warning scraping: {e}")

          print(f"⬇️ Downloading Target...")
          try:
              with scraper.get(url, stream=True, timeout=120) as r:
                  if r.status_code != 200: sys.exit(1)
                  with open(filename, 'wb') as f:
                      for chunk in r.iter_content(chunk_size=1024*1024): 
                          if chunk: f.write(chunk)
          except Exception as e:
              print(f"❌ Error: {e}")
              sys.exit(1)

      # FIX: Use Python/Cloudscraper for Module too (Wget fails on Mediafire)
      - name: Download Android Faker Module
        shell: python
        run: |
          import cloudscraper
          import sys
          from bs4 import BeautifulSoup

          url = "https://www.mediafire.com/file/32gssatb1p4pkbk/Android_Fakers.apk/file"
          scraper = cloudscraper.create_scraper(browser='chrome')
          filename = "module.apk"

          print(f"🔗 Processing Module URL...")
          try:
              html = scraper.get(url).text
              soup = BeautifulSoup(html, 'html.parser')
              download_btn = soup.find('a', {'id': 'downloadButton'})
              if download_btn:
                  url = download_btn.get('href')
          except Exception as e:
              print(f"⚠️ Warning scraping module: {e}")

          print(f"⬇️ Downloading Module...")
          try:
              with scraper.get(url, stream=True, timeout=120) as r:
                  if r.status_code != 200: 
                      print(f"❌ Failed status: {r.status_code}")
                      sys.exit(1)
                  with open(filename, 'wb') as f:
                      for chunk in r.iter_content(chunk_size=1024*1024): 
                          if chunk: f.write(chunk)
              print("✅ Module Downloaded Successfully")
          except Exception as e:
              print(f"❌ Error downloading module: {e}")
              sys.exit(1)

      - name: Prepare Workspace
        run: |
          mkdir -p original_workspace workspace_reqable workspace_lspatch
          
          # Check if file exists and is not empty
          if [ ! -s "download.apks" ]; then
             echo "❌ Error: download.apks is missing or empty"
             exit 1
          fi

          if unzip -t download.apks >/dev/null 2>&1; then
              echo "📦 File is Archive (XAPK/APKS)"
              unzip -q download.apks -d original_workspace
          else
              echo "📦 File is Single APK"
              mv download.apks original_workspace/base.apk
          fi
          
          if [ ! -f "original_workspace/base.apk" ]; then
             echo "❌ Error: base.apk not found in extracted files!"
             ls -R original_workspace
             exit 1
          fi

          cp -r original_workspace/* workspace_reqable/
          cp -r original_workspace/* workspace_lspatch/

      - name: "Mode 1 - Reqable Patching"
        id: reqable
        continue-on-error: true
        run: |
          cd workspace_reqable
          echo "🔵 Patching Reqable..."
          
          # Increased memory limit for node to prevent crash
          export NODE_OPTIONS="--max-old-space-size=4096"
          
          apk-mitm base.apk --certificate ../reqable.pem
          
          if [ -f "base-patched.apk" ]; then
             mv base-patched.apk base.apk
             for apk in *.apk; do
                zipalign -p -f -v 4 "$apk" "aligned_$apk"
                mv "aligned_$apk" "$apk"
                apksigner sign --ks ../debug.keystore --ks-pass pass:android "$apk"
             done
             zip -r -q ../Reqable_Version.apks .
             echo "✅ Reqable Success"
          else
             echo "❌ Reqable Patch Failed (Check logs for apk-mitm error)"
             exit 1
          fi

      - name: "Mode 2 - LSPatch Patching"
        id: lspatch
        continue-on-error: true
        run: |
          cd workspace_lspatch
          echo "🟣 Patching LSPatch..."
          
          if [ ! -s "../module.apk" ]; then
             echo "❌ Module APK is missing or invalid"
             exit 1
          fi

          java -jar ../lspatch.jar base.apk -m ../module.apk -f -l 2
          
          TARGET=$(find . -name "*-lspatched.apk")
          if [ ! -z "$TARGET" ]; then
             mv "$TARGET" base.apk
             for apk in *.apk; do
                zipalign -p -f -v 4 "$apk" "aligned_$apk"
                mv "aligned_$apk" "$apk"
                apksigner sign --ks ../debug.keystore --ks-pass pass:android "$apk"
             done
             zip -r -q ../LSPatch_Version.apks .
             echo "✅ LSPatch Success"
          else
             echo "❌ LSPatch Failed"
             exit 1
          fi

      - name: Upload Results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Super_Patcher_Results
          path: |
            Reqable_Version.apks
            LSPatch_Version.apks
