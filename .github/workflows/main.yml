name: Super Patcher V10 (Arch Checker + Android 15 Fix)

on:
  workflow_dispatch:
    inputs:
      apk_url:
        description: 'Masukkan Link APK (Cari versi armeabi-v7a / Universal!)'
        required: true
        type: string
        default: 'https://www.mediafire.com/file/lxciw8l36u045wn/Mnet+Plus_3.38.0.apk/file'

jobs:
  inject-everything:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java (Temurin 17)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10' 

      # Install AAPT untuk cek arsitektur (32 vs 64 bit)
      - name: Install Android Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y aapt

      - name: Create Certificate File
        run: |
          cat <<EOF > reqable.pem
          -----BEGIN CERTIFICATE-----
          
          -----END CERTIFICATE-----
          EOF

      - name: Install Tools
        run: |
          npm install -g apk-mitm
          wget "https://github.com/LSPosed/LSPatch/releases/download/v0.6/jar-v0.6-398-release.jar" -O lspatch.jar
          wget "https://github.com/patrickfav/uber-apk-signer/releases/download/v1.3.0/uber-apk-signer-1.3.0.jar" -O signer.jar
          pip install cloudscraper requests beautifulsoup4

      - name: Download APK
        shell: python
        env:
          APK_URL: ${{ inputs.apk_url }}
        run: |
          import cloudscraper
          import os
          import sys
          import re
          from bs4 import BeautifulSoup
          from urllib.parse import urlparse

          original_url = os.environ['APK_URL']
          url = original_url
          scraper = cloudscraper.create_scraper(browser='chrome')

          print(f"🔗 Menganalisis URL: {url}")
          need_scrape = False
          if 'mediafire.com' in url:
              if 'download' in urlparse(url).netloc:
                  need_scrape = False
              elif '/file/' in url:
                  need_scrape = True
              else:
                  need_scrape = True

          if need_scrape:
              try:
                  html = scraper.get(url).text
                  soup = BeautifulSoup(html, 'html.parser')
                  download_btn = soup.find('a', {'id': 'downloadButton'})
                  if download_btn and download_btn.get('href'):
                      url = download_btn.get('href')
                      if url.startswith('//'): url = 'https:' + url
                      print(f"✅ Direct Link ditemukan: {url}")
                  else:
                      match = re.search(r'href="((https?:)?//download[^"]+)"', html)
                      if match:
                          url = match.group(1)
                          if url.startswith('//'): url = 'https:' + url
                          print(f"✅ Direct Link (Regex) ditemukan: {url}")
                      else:
                          print("❌ FATAL: Gagal Scrape.")
                          sys.exit(1)
              except Exception as e:
                  print(f"❌ Error Scrape: {str(e)}")
                  sys.exit(1)

          print(f"⬇️ Sedang mendownload dari: {url}")
          try:
              with scraper.get(url, stream=True, timeout=60) as r:
                  if r.status_code != 200:
                      print(f"❌ Gagal akses URL: {r.status_code}")
                      sys.exit(1)
                  total_size = int(r.headers.get('content-length', 0))
                  with open('target.apk', 'wb') as f:
                      dl_size = 0
                      for chunk in r.iter_content(chunk_size=1024*1024): 
                          if chunk:
                              f.write(chunk)
                              dl_size += len(chunk)
                              if total_size > 0 and dl_size % (10 * 1024 * 1024) == 0:
                                  print(f"   ... {dl_size/1024/1024:.2f} MB")
                  print(f"✅ Download Selesai. Total: {dl_size/1024/1024:.2f} MB")
                  if dl_size < 1048576: 
                      print("❌ ERROR: File < 1MB.")
                      sys.exit(1)
          except Exception as e:
              print(f"❌ Download Gagal: {str(e)}")
              sys.exit(1)

      - name: Check APK Architecture (PENTING!)
        run: |
          echo "🔍 Menganalisis Arsitektur APK..."
          ARCH_INFO=$(aapt dump badging target.apk | grep "native-code")
          echo "📋 Arsitektur yang didukung APK ini: $ARCH_INFO"
          
          if [[ "$ARCH_INFO" == *"arm64"* ]] && [[ "$ARCH_INFO" != *"armeabi-v7a"* ]]; then
            echo "================================================================"
            echo "⚠️  PERINGATAN KERAS: APK INI HANYA 64-BIT (arm64-v8a)!"
            echo "⚠️  Jika Realme Note 50 Anda menggunakan OS 32-bit (sangat umum),"
            echo "⚠️  APK ini PASTI GAGAL DIINSTALL."
            echo "⚠️  Solusi: Cari link download versi 'armeabi-v7a' atau 'Universal'."
            echo "================================================================"
          elif [[ "$ARCH_INFO" == *"armeabi-v7a"* ]]; then
            echo "✅ Kabar Baik: APK ini mengandung library 32-bit. Harusnya kompatibel!"
          else
            echo "ℹ️ Info: APK ini mungkin Universal atau tidak ada native libs."
          fi

      - name: Download Android Faker
        run: |
          LATEST_URL=$(curl -s https://api.github.com/repos/Android1500/AndroidFaker/releases/latest | grep "browser_download_url" | grep ".apk" | cut -d '"' -f 4)
          if [ -z "$LATEST_URL" ]; then
             wget "https://github.com/Android1500/AndroidFaker/releases/download/1.2.0/AndroidFaker_1.2.0.apk" -O module.apk
          else
             wget "$LATEST_URL" -O module.apk
          fi

      - name: Tahap 1 - Patch Network (Reqable)
        run: |
          apk-mitm target.apk --certificate reqable.pem
          mv *-patched.apk step1.apk

      - name: Tahap 2 - Inject Spoofer (LSPatch)
        run: |
          java -jar lspatch.jar step1.apk -m module.apk -f -l 2
          
          OUTPUT_LSPATCH=$(find . -maxdepth 1 -type f -name "*-lspatched.apk" | head -n 1)
          if [ -z "$OUTPUT_LSPATCH" ]; then
             OUTPUT_LSPATCH=$(ls -t *.apk | grep -v "step1.apk" | grep -v "module.apk" | grep -v "target.apk" | head -n 1)
          fi
          
          if [ -z "$OUTPUT_LSPATCH" ]; then
             echo "❌ Gagal patch!"
             exit 1
          fi
          mv "$OUTPUT_LSPATCH" unsigned_final.apk

      - name: Tahap 3 - Final Signing (Android 15 Fix)
        run: |
          echo "🔏 Signing Ulang (Uber Signer V2+V3)..."
          java -jar signer.jar --apks unsigned_final.apk
          
          FINAL_APK=$(find . -maxdepth 1 -type f -name "*-aligned-debugSigned.apk" | head -n 1)
          if [ -z "$FINAL_APK" ]; then
             echo "❌ Gagal Signing."
             exit 1
          fi
          
          # Rename hasil akhir agar mudah dikenali
          NEW_NAME="MnetPlus_Patched_A15.apk"
          mv "$FINAL_APK" "$NEW_NAME"
          
          echo "✅ Selesai! File: $NEW_NAME"

      - name: Upload Hasil Akhir
        uses: actions/upload-artifact@v4
        with:
          name: MnetPlus_Patched_A15
          path: MnetPlus_Patched_A15.apk
